# Emoji Maker AI - MVP 需求文档

## 一、产品概述

**产品名称：** Emoji Maker AI
**目标用户：** 内容创作者、社交媒体用户、需要个性化 emoji 的普通用户
**核心价值：** 输入一段文字描述，AI 自动生成专属 emoji 风格图片，一键下载使用
**部署方式：** Cloudflare Pages（前端）+ Cloudflare Workers（API 代理）
**AI 模型：** Replicate `fofr/emoji-maker`

---

## 二、MVP 功能范围

### 2.1 核心功能：文字生成 Emoji
- 用户输入文字描述（中文/英文均可）
- 点击生成按钮，调用 AI 模型生成 emoji 风格图片
- 展示生成结果（支持多张候选图）
- 一键下载 PNG

### 2.2 生成参数控制
- **风格选择：** emoji / 3D emoji / 像素风 / 贴纸风
- **表情选择：** 开心 / 悲伤 / 愤怒 / 惊讶 / 酷 / 默认
- 生成数量：1 张（MVP 阶段）

### 2.3 历史记录（内存级）
- 本次会话内保留最近 6 张生成记录
- 点击历史图片可重新下载
- 刷新页面后清空（不持久化）

### 2.4 免费额度控制
- 每个 IP 每天免费生成 3 次
- 超出后提示升级（MVP 阶段仅展示提示，不接入支付）
- 额度存储：Cloudflare KV

---

## 三、用户流程

```
用户访问页面
    ↓
输入文字描述（如"一只戴墨镜的猫"）
    ↓
选择风格 / 表情（可选）
    ↓
点击「生成 Emoji」
    ↓
等待 AI 生成（约 10-20 秒，显示进度动画）
    ↓
展示生成结果
    ↓
下载 PNG / 继续生成
```

---

## 四、页面结构

```
┌─────────────────────────────────────────┐
│         🎨 Emoji Maker AI               │
│      用 AI 生成你的专属 Emoji            │
├─────────────────────────────────────────┤
│                                         │
│  描述你想要的 Emoji：                    │
│  ┌─────────────────────────────────┐    │
│  │ 一只戴墨镜的猫咪...              │    │
│  └─────────────────────────────────┘    │
│                                         │
│  风格：[Emoji] [3D] [像素] [贴纸]        │
│  表情：[😊] [😢] [😎] [😮] [😡]         │
│                                         │
│  ┌──────────────────────────────┐       │
│  │      ✨ 生成 Emoji           │       │
│  └──────────────────────────────┘       │
│                                         │
├─────────────────────────────────────────┤
│  生成结果：                              │
│  ┌──────┐                               │
│  │ 图片 │  [⬇️ 下载]                    │
│  └──────┘                               │
│                                         │
│  最近生成：                              │
│  [图1] [图2] [图3] [图4] [图5] [图6]    │
└─────────────────────────────────────────┘
```

---

## 五、技术方案

| 模块 | 技术 |
|------|------|
| 前端 | 原生 HTML/CSS/JavaScript |
| 后端 | Cloudflare Workers（代理 Replicate API） |
| AI 模型 | Replicate `fofr/emoji-maker` |
| 额度控制 | Cloudflare KV（IP 维度，每日重置） |
| 图片存储 | 无（直接返回 Replicate 图片 URL，内存展示） |
| 部署 | Cloudflare Pages + Workers |

### API 调用流程
```
前端 → Cloudflare Worker → Replicate API → 返回图片 URL → 前端展示
```

Worker 的作用：
1. 隐藏 Replicate API Token（不暴露在前端）
2. 做 IP 限流（读写 KV）
3. 处理跨域问题

---

## 六、非功能需求

- **生成时间：** AI 生成约 10-20 秒，前端显示进度动画
- **兼容性：** 支持 Chrome/Safari/Firefox 最新版
- **移动端：** 响应式布局，手机可正常使用
- **隐私：** 用户输入不存储，仅用于当次 API 调用

---

## 七、不在 MVP 范围内

- 用户登录/注册
- 付费订阅（仅展示升级提示）
- 图片编辑功能
- 社交分享
- 多语言支持
- 批量生成

---

## 八、所需资源

| 资源 | 获取方式 | 费用 |
|------|---------|------|
| Replicate API Token | 注册 replicate.com | 免费额度约 $5 |
| Cloudflare 账号 | 已有 | 免费 |
| Cloudflare KV | Workers 控制台创建 | 免费（10万次/天） |

---

## 九、开发计划

| 阶段 | 内容 | 预计时间 |
|------|------|---------|
| Phase 1 | 前端页面 + 基础交互 | 0.5 天 |
| Phase 2 | Cloudflare Worker + Replicate API 接入 | 0.5 天 |
| Phase 3 | IP 限流 + KV 配置 | 0.5 天 |
| Phase 4 | 样式优化 + 移动端适配 | 0.5 天 |
| Phase 5 | Cloudflare 部署上线 | 0.5 天 |

**总计：约 2-3 天**
